# ===========================================
# Database (Vercel Postgres / Neon)
# ===========================================
# These are automatically set by Vercel when you connect a Postgres database
# via the Vercel Marketplace (Neon, Supabase, etc.)

# Pooled connection URL (for regular queries in serverless)
# Format: postgresql://user:password@hostname-pooler.region.neon.tech/dbname?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/wheeldo"

# Direct connection URL (for Prisma migrations)
# Format: postgresql://user:password@hostname.region.neon.tech/dbname?sslmode=require
DIRECT_URL="postgresql://user:password@localhost:5432/wheeldo"

# Alternative names Vercel may provide (use DATABASE_URL and DIRECT_URL above):
# POSTGRES_URL - Same as DATABASE_URL (pooled)
# POSTGRES_URL_NON_POOLING - Same as DIRECT_URL
# POSTGRES_PRISMA_URL - Pooled URL with pgbouncer params

# Better Auth - GitHub OAuth
# Create OAuth App at: https://github.com/settings/developers
# Homepage URL: http://localhost:3000
# Callback URL: http://localhost:3000/api/auth/callback/github
BETTER_AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_GITHUB_ID="your-github-oauth-app-id"
AUTH_GITHUB_SECRET="your-github-oauth-app-secret"

# ===========================================
# Geoapify (Location Autocomplete)
# ===========================================
NEXT_PUBLIC_GEOAPIFY_API_KEY="your-geoapify-api-key"
